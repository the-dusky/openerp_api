<h1>Listing open_erps</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @open_erps.each do |open_erp| %>
  <tr>
    <td><%= link_to 'Show', open_erp %></td>
    <td><%= link_to 'Edit', edit_open_erp_path(open_erp) %></td>
    <td><%= link_to 'Destroy', open_erp, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Open erp', new_open_erp_path %>
